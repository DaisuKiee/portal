# Server Configuration
PORT=5000

# Shared MongoDB Database
# Everyone on the team uses this same URI
# Make sure your IP is whitelisted in MongoDB Atlas Network Access
MONGODB_URI=mongodb+srv://tour:power@tour.5o5ufwf.mongodb.net/ctu-admission?retryWrites=true&w=majority

# JWT Secret (use same as team for token compatibility)
JWT_SECRET=ctu_admission_portal_secret_key_2026

# Email Configuration (Optional - use your own Gmail or skip for testing)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Email Configuration Instructions:
# 1. Go to Google Account settings
# 2. Enable 2-Step Verification
# 3. Generate App Password for "Mail"
# 4. Use that app password here (not your regular Gmail password)
# 5. OR leave as dummy values for testing (email features won't work)
